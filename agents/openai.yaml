interface:
  display_name: "Dimensional Rift Scan Ribbon"
  short_description: "Create borderless registered scan-ribbon prompts"
  default_prompt: "Use $dimensional-rift-prompt to create a duration-adaptive bilingual prompt for one borderless opaque horizontal scan ribbon, registered alternate body regions, hands always outside both ends, readable gesture-driven folding when requested, and a complete footprint no larger than 25% of the frame. Do not prescribe the video ratio."
